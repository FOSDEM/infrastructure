---
- name: create mountpoint
  file: path=/mnt/video state=directory owner=root group=root mode=0777

# TODO:
# - check if /dev/sdwhatever is present
# - check if it has a filesystem, if it hasn't got one, make one
# - mount it on /mnt/vide and add it to fstab

- name: create video directories per room
  file: path=/mnt/video/{{ item.room }} state=directory owner=root group=root mode=0777
  with_items: "{{ vocto_containers }}"
