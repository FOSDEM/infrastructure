# Install base role everywhere
- hosts: all
  tags: [base]
  roles:
    - users
    - base
# Playbook for installing event nodes
- hosts: event
  tags: [docker]
  roles:
    - docker
    - prometheus.docker
    - dns
