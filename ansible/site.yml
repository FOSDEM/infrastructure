# Install base role everywhere
- hosts: all
  tags: [base]
  roles:
    - docker
    - users
    - base
# Playbook for installing event primary node
- hosts: event-primary
  tags: [event-primary]
  roles:
    - prometheus.docker
    - dns
