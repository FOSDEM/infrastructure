# Install base role everywhere
- hosts: all
  tags: [base]
  roles:
    - base
# Playbook for installing event nodes
- hosts: event
  tags: [docker]
  roles:
    - docker
    - users
    - prometheus.docker
    - dns
