[defaults]
inventory = hosts
host_key_checking = False
ansible_user = root
roles_path = playbooks/roles
forks = 200
nocows = 1
vault_password_file = ~/.fosdem_vault_pass.txt

[ssh_connection]
pipelining=True
