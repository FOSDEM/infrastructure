---
- name: enable audioctl
  service:
    name: audioctl
    enabled: true
    state: restarted

- name: install nginx config for interface
  copy:
    src: nginx/interface.conf
    dst: /etc/nginx/sites-enabled/interface.conf
  register: nginx_config
