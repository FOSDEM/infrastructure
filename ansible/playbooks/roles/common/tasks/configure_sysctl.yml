---
- name: configure sysctl
  template:
    src: sysctl/10-fosdem.conf
    dest: /etc/sysctl.d/10-fosdem.conf
    owner: root
    group: root
    mode: 0644
  notify: reload sysctl
  when: sysctl_parameters is defined
