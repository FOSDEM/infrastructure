---
- name: restart chrony
  systemd:
    daemon_reload: true
    name: chrony.service
    state: restarted

- name: reload ssh
  systemd:
    daemon_reload: true
    name: sshd
    state: reloaded

- name: reload sysctl
  become: true
  command: /sbin/sysctl -p
