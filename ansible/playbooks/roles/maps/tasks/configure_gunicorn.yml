- name: install the c3nav gunicorn service
  copy:
    src: gunicorn-c3nav.service
    dest: /etc/systemd/system
    owner: root
    group: root
    mode: 0644

- name: enable c3nav gunicorn service
  service:
    name: gunicorn-c3nav
    state: started
    enabled: yes
    daemon_reload: yes

