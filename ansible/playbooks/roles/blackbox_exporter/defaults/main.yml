blackbox_exporter_install_path: /srv/blackbox_exporter
blackbox_exporter_version: 0.11.0
blackbox_exporter_download_url: https://github.com/prometheus/blackbox_exporter/releases/download

blackbox_exporter_listen_address: "localhost:9115"

blackbox_exporter_tarball_url: "{{ blackbox_exporter_download_url }}/v{{ blackbox_exporter_version }}/blackbox_exporter-{{ blackbox_exporter_version }}.linux-{{ ansible_to_go_arch[ansible_architecture] | default(ansible_architecture) }}.tar.gz"
blackbox_exporter_path: "{{ blackbox_exporter_install_path }}/blackbox_exporter-{{ blackbox_exporter_version }}.linux-{{ ansible_to_go_arch[ansible_architecture] | default(ansible_architecture) }}"
