---

sysctl_parameters:
  net.ipv4.tcp_keepalive_time: 10
  net.ipv4.tcp_keepalive_intvl: 5
  net.ipv4.tcp_keepalive_probes: 5

# Disable hdd config on external.
video_stream_dump_configure_hdd: false
